# Reproducibility environment for "Process Over Edge".
# These are the exact live-environment versions the published results were produced
# on (Python 3.10.12). Older point releases generally work; pin to these to reproduce
# the paper's numbers bit-for-bit where determinism is claimed.

# --- Core scientific stack ---
numpy==2.2.6
scipy==1.15.3
pandas==2.3.3
scikit-learn==1.7.2
numba==0.65.1
matplotlib==3.10.9
pyarrow>=15.0
joblib>=1.3
statsmodels>=0.14

# --- Gradient-boosted trees (cross-sectional rotation study + apex audit) ---
lightgbm==4.6.0
xgboost==3.2.0
catboost==1.2.10
